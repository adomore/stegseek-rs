[package]
name = "stegseek-jpeg"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "JPEG quantized DCT coefficient read + lossless re-encode (pure Rust, no libjpeg)."
